CS 5720 700688700 Nathanael Nading Quiz ICP

All scripts can be run with the following command:

python [fileName].py

They will all output a model with the following name ([fileName] is the name of the script):

[fileName].keras
